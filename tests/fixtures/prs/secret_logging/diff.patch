diff --git a/app/auth/handler.py b/app/auth/handler.py
index 8c2f1a0..d4e9b73 100644
--- a/app/auth/handler.py
+++ b/app/auth/handler.py
@@ -1,5 +1,6 @@
 import logging
 from app.models import Session
+from app.config import settings

 logger = logging.getLogger(__name__)

@@ -10,8 +11,12 @@ def authenticate(request):
     token = request.headers.get("Authorization", "").replace("Bearer ", "")
     if not token:
         return None
+
+    logger.info(f"Auth attempt with token: {token}")
+
     session = Session.query.filter_by(access_token=token).first()
     if session and not session.is_expired():
+        logger.info(f"Authenticated user {session.user.email} with API key {session.api_key}")
         return session.user

     return None
